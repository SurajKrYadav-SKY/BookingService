# Commands to add the projects to the github repository

```
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/<<github-user-id>>/<<repo-name>>.git
git push -u origin main

```
